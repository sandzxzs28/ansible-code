name:  Configuration ansible.cfg 

on:
  workflow_dispatch:

jobs:
  generate-ansible-cfg:
    runs-on: [ ansible-master ]

    steps:


      - name: Generate ansible.cfg
        run: |
          sudo -u sandz bash -c 'cat > /home/sandz/ansible/ansible.cfg' << 'EOF'
          [defaults]
          inventory      = /home/sandz/ansible/inventory.yaml
          roles_path    = /home/sandz/ansible/roles
          host_key_checking = False
          remote_user = sandz
          private_key_file = /home/sandz/.ssh/id_rsa
          log_path = /home/sandz/ansible/ansible.log
          forks = 5
          gathering = smart
          fact_caching = jsonfile
          fact_caching_connection = /home/sandz/ansible/facts_cache
          

          [privilege_escalation]
          become = True
          become_method = sudo
          become_user = root
          become_ask_pass = False

          [ssh_connection]
          ssh_args = -o ControlMaster=auto -o ControlPersist=60s -o ConnectTimeout=10
          control_path = /home/sandz/.ssh/id_rsa-%%r@%%h:%%p
          EOF
          

      - name: Set proper permissions
        run: |
          sudo chown sandz:sandz /home/sandz/ansible/ansible.cfg
          sudo chmod 644 /home/sandz/ansible/ansible.cfg

      - name: Verify configuration
        run: |
          echo "Ansible configuration:"
          sudo -u sandz ls -la /home/sandz/ansible/ansible.cfg
          echo -e "\nContents:"
          sudo -u sandz cat /home/sandz/ansible/ansible.cfg
